version: '3.8'

services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        - VITE_CONVEX_URL=${VITE_CONVEX_URL}
    container_name: edusys-web
    ports:
      - "${WEB_PORT:-3000}:80"
    restart: unless-stopped